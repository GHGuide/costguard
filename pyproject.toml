[project]
name = "costguard_gate"
version = "0.0.3"
description = "CostGuard agentic cost and efficiency regression gate (UiPath AgentHack 2026, Track 3)"
authors = [{ name = "Leonardo Calancea" }]
dependencies = [
    "uipath>=2.11.0, <2.12.0"
]
requires-python = ">=3.11"
