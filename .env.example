# Copy to .env and fill in. NEVER commit .env. The offline demo needs none of these.

# Real LLM gateway (only when swapping MockGateway for a live provider)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# UiPath (only for the platform integration phase)
UIPATH_URL=
UIPATH_CLIENT_ID=
UIPATH_CLIENT_SECRET=
UIPATH_ORG_ID=
UIPATH_TENANT=
